# Benchmarks

Relay will publish reproducible latency, throughput, recovery, and memory
benchmarks. Every result must record the Relay revision, workload, hardware,
operating system, message size, durability mode, and client concurrency.

Comparisons with other brokers must use equivalent guarantees and clearly
state configuration differences.
