# Client SDKs

The first supported client will be a Rust SDK introduced after the TCP protocol
stabilizes. Additional language SDKs are outside the initial scope.

Each SDK must be tested against the same protocol conformance suite.
